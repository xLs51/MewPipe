{% spaceless %}
    {% if not form.vars.valid %}
        <div class="alert alert-danger" role="alert">
            {{ form_errors(form) }}
            {% for children in form.children %}
                {% if not children.vars.valid %}
                    {{ form_errors(children) }}
                {% endif %}
            {% endfor %}
        </div>
    {% endif %}
{% endspaceless %}

<h1>Register</h1>

<table class="table">
    <form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
        <tr>
            <td>Username</td>
            <td>{{ form_widget(form.username, {'attr': {'class' : 'form-control'}}) }}</td>
        </tr>
        <tr>
            <td>Email</td>
            <td>{{ form_widget(form.email, {'attr': {'class' : 'form-control'}}) }}</td>
        </tr>
        <tr>
            <td>Password</td>
            <td>{{ form_widget(form.plainPassword.first, {'attr': {'class' : 'form-control'}}) }}</td>
        </tr>
        <tr>
            <td>Confirm</td>
            <td>{{ form_widget(form.plainPassword.second, {'attr': {'class' : 'form-control'}}) }}</td>
        </tr>
        <tr>
          <td colspan="2"><button class="btn btn-default" type="submit">Submit !</button></td>
        </tr>

        {{ form_rest(form) }}
    </form>
</table>
